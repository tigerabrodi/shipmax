{
  "bunVersion": "1.x",
  "installCommand": "bun install",
  "buildCommand": "npx convex deploy --cmd 'bun run build'",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
